#!/usr/bin/python3
def is_same_class(obj, a_class):

    '''method that checks if an object
    is exacty the same instance of the 
    class specified
    '''
    if type(obj) == a_class:
        return True
